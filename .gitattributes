# .cmd 는 반드시 CRLF 로 체크아웃돼야 한다.
# cmd.exe 는 goto/라벨을 만나면 파일을 바이트 오프셋으로 되짚어 다음 줄을 찾는데,
# 그 계산이 줄바꿈을 CRLF(2바이트)로 전제한다. LF 만 있는 .cmd 는 실행 도중 파싱 위치가
# 어긋나 파일이 조각나 실행된다 — 내용이 멀쩡해도 그렇다.
# 이게 없으면 체크아웃하는 PC·설정에 따라 그 PC 에서만 Claude Code 버튼이 깨진다.
*.cmd text eol=crlf
